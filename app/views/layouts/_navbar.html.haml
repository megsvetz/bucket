%nav.navbar.navbar-inverse{:style => "height: 20%"}
  .navbar-header
    \-->
    / <row>
    = link_to image_tag("logo_inv.png", width: "18%"), root_path, justify: "left", display: "inline-block"
    / <row>
    %ul.nav.navbar-nav.navbar-right{:style => "display: inline-block"}
      - if user_signed_in?
        %li.dropdown
          %a.dropdown-toggle{"aria-expanded" => "false", "aria-haspopup" => "true", "data-toggle" => "dropdown", :href => "#", :role => "button"}
            My Buckets
            %span.caret
          %ul.dropdown-menu
            %li= link_to( 'See My Buckets', categories_path )
            %li= link_to( 'Add Buckets', new_category_path )
            %li= link_to( 'Edit profile', edit_user_path(current_user) )
        %li= link_to('Inspiration', users_path)
        %li.header-font= link_to "Logout", destroy_user_session_path, method: :delete, :class => 'header-font'
      - else
        %li.header-font= link_to "Sign up", new_user_registration_path, :class => 'header-font'
        %li.header-font= link_to "Login", new_user_session_path, :class => 'header-font'
        %li     / </row>
    / </row>
    